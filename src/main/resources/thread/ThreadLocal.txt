每一个thread都有一个ThreadLocalMap对象，ThreadLocalMap的key为Threadlocal对象。调用threadLocal的set/get方法实际上是对这个map进行get/set操作。
map中的每一个元素是一个entry对象，entry继承自WeakReference，所以entry中的key为弱引用并且每一次gc都会被回收。如果没有调用remove方法，那么key被回收以后value还是会存在的，
因此value的存在会产生内存泄漏的问题。